name: golangci-lint
on:
  push:
    branches:
      - master
      - main
  pull_request:

jobs:
  test:
    uses: cyverse-de/github-workflows/.github/workflows/go-test.yml@go-test
    with:
      go-version: 1.26
